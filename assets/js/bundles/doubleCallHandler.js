const callDouble = (bundleName, bundleSrc, BundleDesc, extra) =>{ //this.lastChild.firstChild.textContent, this.firstChild.src, this.childNodes[3].textContent, this
    shiftBundleView(bundleName, bundleSrc, BundleDesc, extra);
}